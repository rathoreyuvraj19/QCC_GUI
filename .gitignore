__pycache__/
*.pyc
.claude/settings.local.json
.claude/scheduled_tasks.lock

# PyInstaller build output / spec, and the RC Settings tab's local runtime
# save file - all machine-specific/generated, not source.
# build/
# dist/
*.spec
rc_settings.json
connection_settings.json

.claude/skills/run-qcc-qtrm-control/shots/

# Default save location for Tools -> Plot Log File… / apps/plot_qcc_log.py
plots/

# Word owner/lock files left while a docx is open in Word
~$*.docx
**/~$*.docx

# QTRM firmware reference (Eclipse SoftConsole project, kept as a sanity
# check against the GUI's protocol implementation) - workspace metadata and
# build output are machine-specific/generated, not source.
QTRM_REMOTE_PROG_CODE/**/.metadata/
QTRM_REMOTE_PROG_CODE/**/Debug/
QTRM_REMOTE_PROG_CODE/**/Release/
